# v0.1.2 validation evidence

This file records the public release evidence for Kern `v0.1.2`.

## Release metadata

- Release tag: `v0.1.2`
- Published: `2026-05-17T14:23:08Z`
- App version: `0.1.2`
- Distribution model: unsigned, not-notarized GitHub DMG plus checksum sidecar

## Published assets

| Asset | Size | Digest |
|---|---:|---|
| `Kern-macOS-Release.dmg` | 1,989,883 bytes | `sha256:14af3434a65168fd2253325201c3fc37b8fe2600029265d2c7f8a14c0eb23006` |
| `Kern-macOS-Release.dmg.sha256` | 89 bytes | `sha256:ab72c25972d4ddabaca98859de1cc09103ac79439780076c464141363c544942` |

Local sidecar content reviewed for the DMG:

```text
14af3434a65168fd2253325201c3fc37b8fe2600029265d2c7f8a14c0eb23006  Kern-macOS-Release.dmg
```

## Automated validation recorded for release

| Gate | Command | Result |
|---|---|---|
| Default native unit/snapshot suite | `./scripts/test-native-editor.sh` | Passed: 434 executed, 79 skipped, 0 failures |
| Strict CommonMark/GFM conformance | `./scripts/test-markdown-spec-conformance.sh` | Passed: 5 selected tests, 0 failures |
| Typing behavior PR gate | `./scripts/run-typing-behavior-gate.sh --lane pr` | Passed: all required factor, edge, pairwise, and critical triple coverage met |
| Benchmark package tests | `cd scripts/kern-bench && swift test -c release` | Passed during release validation |
| Local packaged app build | `./scripts/package-kern-app.sh` | Produced `dist/Kern.app`, DMG, and checksum sidecar |
| Local checksum | `(cd dist && shasum -a 256 -c Kern-macOS-Release.dmg.sha256)` | Passed |
| DMG inspection | `hdiutil attach` / inspect / detach | Expected `Kern.app` plus `Applications` symlink |
| Published asset verification | `./scripts/verify-github-release-asset.sh v0.1.2` | Published DMG and checksum sidecar matched the reviewed local digest |
| Signing/Gatekeeper posture | `spctl -a -vv dist/Kern.app` | Rejected as expected for unsigned/not-notarized app |

## Evidence notes

The ignored local evidence tree contains the release logs and result bundles from the release run. These are not tracked because `test-results/`, `dist/`, and Xcode result bundles are generated artifacts.

The release remains intentionally unsigned and not notarized. The checksum proves same-release asset integrity only; it does not authenticate publisher identity the way Apple signing and notarization would.
